<%= form_with(model: markdown_code) do |form| %>
  <% if markdown_code.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(markdown_code.errors.count, "error") %> prohibited this markdown_code from being saved:</h2>

      <ul>
        <% markdown_code.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
